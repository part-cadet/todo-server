name: CI

on:
  push:
    branches:
      - master

jobs:
  start:
    name: start and test
    runs-on: ubuntu-latest

    steps:
    - name: Run a multi-line script
      run: |
        npm install
        npm start --if-present
        npm test
